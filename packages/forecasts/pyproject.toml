[project]
name = "forecasts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Flavien Lambert", email = "flavien.lambert@sencrop.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
forecasts = "forecasts:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
